import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    titleMain:{
      marginTop: 50,
      fontSize: 20,
      textAlign: 'center'
    },
    container: {
      flex: 1,
      backgroundColor: '#cbf7ed',
    
    },
  });